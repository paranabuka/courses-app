<% content_for :title do %>
  Review
  <%= @enrollment %>
<% end %>
<div class="card">
  <div class="card-header">
    <h3>
      <i class="text-warning fa-solid fa-star"></i>
      Add review
    </h3>
  </div>
  <div class="card-body">
    <%= render 'form', enrollment: @enrollment %>
  </div>
  <div class="card-footer">
    <%= link_to 'Back', course_path(@enrollment.course) %>
  </div>
</div>
